# 安全策略

请不要在公开 Issue、Pull Request 或提交中披露可利用的漏洞、凭据或真实业务数据。优先通过 GitHub Security Advisories 的私密报告入口提交；入口不可用时，先联系仓库维护者建立私密沟通。

报告应包含受影响版本或提交、影响范围、复现步骤、脱敏证据和建议修复窗口。不要附加 API key、数据库密码、访问令牌、客户数据或完整生产配置。

backend/.env 和其他本地配置必须保持在 Git 忽略范围内，只提交脱敏示例配置。源系统、源表和生产数据库保持只读；任何写入都必须经过明确的审批、发布和审计边界。AI 输出只能作为规则草案或证据，不得绕过回放、人工审批和正式发布门禁。